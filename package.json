{
  "name": "senq",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:web": "bun run --filter @senq/web dev",
    "dev:api": "bun run --filter @senq/api dev",
    "build": "bun run --filter '*' build",
    "build:web": "bun run --filter @senq/web build",
    "build:api": "bun run --filter @senq/api build",
    "lint": "bun run --filter '*' lint"
  }
}
